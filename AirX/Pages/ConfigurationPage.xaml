<Page
    x:Class="AirX.Pages.ConfigurationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AirX.Pages"
    xmlns:ui="using:CommunityToolkit.WinUI.UI"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="ViewModel"
>

    <StackPanel>
        <StackPanel Margin="20, 40, 10, 0">
            <TextBlock Text="Preferences" FontSize="24" FontWeight="Bold"/>
        </StackPanel>
       
        <Pivot Margin="20, 20, 20, 0">
            <PivotItem Header="AirX Core" Margin="8">
                <StackPanel Spacing="4">
                    <labs:SettingsCard Header="LAN Discovery Server Port" Description="1024 ~ 65535" IsClickEnabled="True" ActionIcon="{ui:FontIcon Glyph={x:Null}}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBox MaxLength="6" Text="{x:Bind ViewModel.LanDiscoveryServerPort, Mode=TwoWay}" />
                            <Button Click="OnLanDiscoveryServerPortSaved">
                                <FontIcon Glyph="&#xE73E;" />
                            </Button>
                        </StackPanel>
                    </labs:SettingsCard>
                    <labs:SettingsCard Header="LAN Discovery Client Port" Description="1024 ~ 65535" IsClickEnabled="True" ActionIcon="{ui:FontIcon Glyph={x:Null}}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBox MaxLength="6" Text="{x:Bind ViewModel.LanDiscoveryClientPort, Mode=TwoWay}" />
                            <Button Click="OnLanDiscoveryClientPortSaved">
                                <FontIcon Glyph="&#xE73E;" />
                            </Button>
                        </StackPanel>
                    </labs:SettingsCard>
                    <labs:SettingsCard Header="Data Service Listen Address (IpV4)" IsClickEnabled="True" ActionIcon="{ui:FontIcon Glyph={x:Null}}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBox MaxLength="6" Text="{x:Bind ViewModel.DataServiceAddressIpV4, Mode=TwoWay}" />
                            <Button Click="OnDataServiceListenAddressSaved">
                                <FontIcon Glyph="&#xE73E;" />
                            </Button>
                        </StackPanel>
                    </labs:SettingsCard>
                    <labs:SettingsCard Header="Data Service Listen Port" Description="1024 ~ 65535" IsClickEnabled="True" ActionIcon="{ui:FontIcon Glyph={x:Null}}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBox MaxLength="6" Text="{x:Bind ViewModel.DataServicePort, Mode=TwoWay}"/>
                            <Button Click="OnDataServiceListenPortSaved">
                                <FontIcon Glyph="&#xE73E;" />
                            </Button>
                        </StackPanel>
                    </labs:SettingsCard>
                    <labs:SettingsCard 
                        Header="Group Identity" 
                        Description="0 ~ 255. Only deivces with the same Group Identity can discover each other. "
                        IsClickEnabled="True" ActionIcon="{ui:FontIcon Glyph={x:Null}}"
                    >
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBox MaxLength="6" Text="{x:Bind ViewModel.GroupIdentity, Mode=TwoWay}"/>
                            <Button Click="OnGroupIdentitySaved">
                                <FontIcon Glyph="&#xE73E;" />
                            </Button>
                        </StackPanel>
                    </labs:SettingsCard>
                    <TextBlock>Saved settings take effect at next launch.</TextBlock>
                </StackPanel>
            </PivotItem>

            <PivotItem Header="Account">
                <StackPanel>
                    <Button Content="Copy Text to the Clipboard" Margin="5,30,0,30" Click="CopyText_Click" FontWeight="Bold"/>
                    <Button Content="Paste Text from the Clipboard" Margin="5,30,0,30" Click="PasteText_Click" FontWeight="Bold"/>
                </StackPanel>
            </PivotItem>

            <PivotItem Header="Customization">
                <StackPanel>
                    <Button x:Name="OpenFolderButton" Click="OpenFolderButton_Click" Content="OpenFolder" Background="MediumSeaGreen" Margin="8,0,0,0" Padding="10" CornerRadius="25"/>
                    <TextBlock x:Name="textBlock" Margin="12,30,0,30" Text="Lan Clipboard Server Port" FontWeight="Bold"/>
                </StackPanel>
            </PivotItem>
        </Pivot>
    </StackPanel>
</Page>
